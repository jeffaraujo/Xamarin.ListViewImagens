<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Xamarin.ListViewImagens"
             x:Class="Xamarin.ListViewImagens.MainPage">

<StackLayout>
    <ListView x:Name="ListViewTexto">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell
                    Text="{Binding Texto}"
                    Detail="{Binding Detalhe}"/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
        <ListView x:Name="ListViewImagem">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell
                        Text="{Binding Texto}"
                        Detail="{Binding Detalhe}"
                        ImageSource="{Binding Imagem}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>